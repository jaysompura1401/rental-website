# MySQL API backend URL (used by the React frontend)
# With Vite proxy, use relative path /api so images + API both work
VITE_API_URL=/api

# Google Maps JavaScript API key
# Get one at https://console.cloud.google.com → Maps JavaScript API
VITE_GOOGLE_MAPS_API_KEY=
